{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The pet window listens for state pushes. It asks for nothing else.",
  "windows": ["clawd"],
  "permissions": ["core:event:default", "core:app:default"]
}
